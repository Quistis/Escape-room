import './empty-quests.css';

const EmptyQuests = (): JSX.Element => (
  <h2 className="empty-quests" style={{textAlign: 'center', gridColumn: '1 / -1', paddingTop: '70px'}}>🥺👉👈Тут пока ничего нет👉👈🥺</h2>
);

export default EmptyQuests;
